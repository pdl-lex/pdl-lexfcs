# MongoDB connection
MONGO_PASSWORD=your_password_here

# Public URL of this endpoint (used in PIDs and self-references)
BASE_URL=http://localhost:8080

# Docker network where adl-mongodb is running
# Find with: docker network ls | grep adl
DOCKER_NETWORK=adl_default
